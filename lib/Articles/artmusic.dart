import 'package:flutter/material.dart';

class MusicArticles extends StatelessWidget {
  const MusicArticles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Music Articles'),
      ),
      body: Placeholder(),
    );
  }
}
