import 'package:flutter/material.dart';

class ThankfulArticles extends StatelessWidget {
  const ThankfulArticles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thankful Articles'),
      ),
      body: Placeholder(),
    );
  }
}
