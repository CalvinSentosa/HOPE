import 'package:flutter/material.dart';

class MeditationArticles extends StatelessWidget {
  const MeditationArticles({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Meditation Articles'),
      ),
      body: Placeholder(),
    );
  }
}
